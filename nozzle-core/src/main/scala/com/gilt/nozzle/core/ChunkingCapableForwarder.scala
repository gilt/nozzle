package com.gilt.nozzle.core

import akka.actor.ActorRef
import spray.client.pipelining.sendReceive

class ChunkingCapableForwarder(ref: ActorRef) {

}

object ChunkingCapableForwarder {
//  def apply(ref: ActorRef): ForwardRequest = {
//    val forwarder = new ChunkingCapableForwarder(ref)
//    sendReceive
//  }
}
